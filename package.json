{
  "name": "otus-graph-flashloan-oracle",
  "version": "1.0.0",
  "description": "Homework project: Chainlink oracle contract, AAVE flashloan strategy, ethers.js script and The Graph indexer.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "copy-abi": "mkdir -p abis && cp artifacts/contracts/FlashLoanOracleStrategy.sol/FlashLoanOracleStrategy.json abis/FlashLoanOracleStrategy.json",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "ethers": "node scripts/ethersClient.js",
    "graph:codegen": "graph codegen",
    "graph:build": "graph build"
  },
  "keywords": [
    "hardhat",
    "chainlink",
    "aave",
    "thegraph",
    "ethers"
  ],
  "license": "MIT",
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.36.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18"
  },
  "dependencies": {
    "ethers": "^6.17.0"
  }
}
